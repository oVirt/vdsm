#!/bin/sh

CLIENT="@VDSMDIR@/vdsClient.py"
[ -e "$CLIENT" ] || CLIENT="$CLIENT"c

[ -n "$PYTHONPATH" ] && PYTHON_FORMATTED_PATH=":$PYTHONPATH"
PYTHONPATH="@VDSMDIR@$PYTHON_FORMATTED_PATH" python "$CLIENT" "$@"
